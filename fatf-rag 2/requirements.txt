# Core RAG stack. Pins are minimum-version (>=) to avoid wheel-availability
# failures across Python versions / platforms. Tested with Python 3.10-3.12.
pdfplumber>=0.11           # PDF text extraction (layout-aware)
sentence-transformers>=2.7 # local embedding models (no API key)
rank-bm25>=0.2.2           # sparse / lexical retrieval for hybrid search
numpy>=1.24,<2.0
scikit-learn>=1.3          # cosine utils + TF-IDF baseline in eval

# LLM generation — two fully-local backends, no API keys:
#  (a) default "hf" backend: open-source GPT-2 / Flan-T5 via transformers below;
#  (b) optional "ollama" backend (set LLM_BACKEND=ollama): a local Llama model
#      served by Ollama (https://ollama.com) over its local HTTP API using only
#      the Python standard library. Install the Ollama app separately.
transformers>=4.40        # GPT-2 / Flan-T5 generators (also used by embeddings)
pypdf>=4.0                # PDF reader used by the scenario notebook
python-dotenv>=1.0        # load optional config overrides from .env
matplotlib>=3.7           # plots in the scenario notebook
seaborn>=0.12             # plots in the scenario notebook

# Interfaces
streamlit>=1.36           # web UI entry-point
jupyter>=1.0              # reproducible notebook
ipykernel>=6.29
pandas>=2.0               # results tables in the notebook
tqdm>=4.66

# OPTIONAL accelerator. The vector store falls back to exact NumPy cosine
# search if faiss is absent (fine for this ~400-chunk corpus), so you can skip
# this line if no wheel is available for your Python. To enable:
#   pip install faiss-cpu        # PyPI wheels: Python <=3.12
#   conda install -c pytorch faiss-cpu   # alternative on 3.13 / Apple Silicon
# faiss-cpu>=1.8
